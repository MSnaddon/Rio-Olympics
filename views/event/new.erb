<h2> new </h2>

<form action="/event" method="post">


{name => <input>}

  <label for="name">Event Name</label>
  <input type="text" name="name">

  <h3>Participants</h3>

  <% @athletes.each do |athlete| %>
  <p>
  <label for="<%= athlete.name %>"><%= athlete.name %></label>
  <input type="checkbox" name="<%= athlete.name %>" value="<%= athlete.id %>">
  </p>

  <% end %>


  <input type="submit" name="add_event">
</form>

<a href="/">Home</a>
<a href="/event">Back</a>